/* Placeholder — overwritten at deploy time by .github/workflows/deploy.yml
 * with the SPOTIFY_CLIENT_ID / SPOTIFY_CLIENT_SECRET repository secrets.
 * Leave empty in the repo; without baked-in values the site falls back to
 * asking for credentials in the browser. */
window.SPOTIFY_CONFIG = {
  clientId: "",
  clientSecret: "",
};
